# Reserved MCP Application Workspace

This directory is reserved for future implementation of the MCP product line.

Suggested submodules:

- `gateway/`
- `worker/`
- `shared-client/`

The MCP product line should reuse the shared backend capabilities of the Web
product line instead of creating a second OCR processing backend.
